/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  
    // Define environment variables to be accessible in the frontend
    env: {
      NEXT_PUBLIC_DOMAIN: "https://sujalsharmaprojects.online",
    },
  };
  
  export default nextConfig;